class Solution {
    fun setZeroes(matrix: Array<IntArray>): Unit {
        
    }
}

// kotlinc setZeroes.kt -include-runtime -d /tmp/29.jar && java -jar /tmp/29.jar
fun main() {
    val matrix: Array<IntArray> = listOf();
    solution.setZeroes(matrix)
    println("results: ${matrix}")
}
